% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fozzie_interval_join.R
\name{fozzie_interval_join_family}
\alias{fozzie_interval_join_family}
\alias{fozzie_interval_join}
\alias{fozzie_interval_inner_join}
\alias{fozzie_interval_left_join}
\alias{fozzie_interval_right_join}
\alias{fozzie_interval_anti_join}
\alias{fozzie_interval_full_join}
\title{Perform a fuzzy join between two data frames using interval containment.}
\usage{
fozzie_interval_join(
  df1,
  df2,
  by,
  how = "inner",
  max_distance = 1,
  distance_col = NULL,
  nthread = NULL
)

fozzie_interval_inner_join(
  df1,
  df2,
  by,
  max_distance = 1,
  distance_col = NULL,
  nthread = NULL
)

fozzie_interval_left_join(
  df1,
  df2,
  by,
  max_distance = 1,
  distance_col = NULL,
  nthread = NULL
)

fozzie_interval_right_join(
  df1,
  df2,
  by,
  max_distance = 1,
  distance_col = NULL,
  nthread = NULL
)

fozzie_interval_anti_join(
  df1,
  df2,
  by,
  max_distance = 1,
  distance_col = NULL,
  nthread = NULL
)

fozzie_interval_full_join(
  df1,
  df2,
  by,
  max_distance = 1,
  distance_col = NULL,
  nthread = NULL
)
}
\arguments{
\item{df1}{A data frame to join from (left table).}

\item{df2}{A data frame to join to (right table).}

\item{by}{A named list or character vector indicating the matching columns. Can be a character vector of length 2, e.g. \code{c("col1", "col2")},
or a named list like \code{list(col1 = "col2")}.}

\item{how}{A string specifying the join mode. One of:
\itemize{
\item \code{"inner"}: matched pairs only.
\item \code{"left"}: all rows from \code{df1}, unmatched rows filled with NAs.
\item \code{"right"}: all rows from \code{df2}, unmatched rows filled with NAs.
\item \code{"full"}: all rows from both \code{df1} and \code{df2}.
\item \code{"anti"}: rows from \code{df1} not matched in \code{df2}.
}}

\item{max_distance}{A numeric threshold for allowable interval expansion (optional).}

\item{distance_col}{Optional name of column to store computed distances.}

\item{nthread}{Optional integer to specify number of threads for parallelization.}
}
\value{
A data frame with approximately matched rows depending on the join type.
}
\description{
\code{fozzie_interval_join()} and its directional variants (\code{fozzie_interval_inner_join()}, \code{fozzie_interval_left_join()}, \code{fozzie_interval_right_join()}, \code{fozzie_interval_anti_join()}, \code{fozzie_interval_full_join()})
enable approximate matching of numeric fields in two data frames based on interval containment logic.
These joins are analogous to \code{fuzzyjoin::interval_join}, but implemented in Rust for performance.
}
\examples{
df1 <- data.frame(x = c(1.5, 2.5, 3.5))
df2 <- data.frame(lower = c(1.0, 2.0, 3.0), upper = c(2.0, 3.0, 4.0))

fozzie_interval_inner_join(df1, df2, by = c("x", "lower"), max_distance = 0.1)

}
